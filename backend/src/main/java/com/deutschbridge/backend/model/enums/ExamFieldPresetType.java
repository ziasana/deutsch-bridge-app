package com.deutschbridge.backend.model.enums;

/** Which exam-exercise form field a saved preset's text is meant for. */
public enum ExamFieldPresetType {
    TEIL_DESCRIPTION,
    DEFAULT_EXPLANATION,
    DEFAULT_COMMON_MISTAKE
}
