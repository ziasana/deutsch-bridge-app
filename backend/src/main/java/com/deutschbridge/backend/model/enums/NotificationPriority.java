package com.deutschbridge.backend.model.enums;

/** Internal ranking only - never exposed to learners. Declaration order is significant (LOW < CRITICAL). */
public enum NotificationPriority {
    LOW,
    MEDIUM,
    HIGH,
    CRITICAL
}
